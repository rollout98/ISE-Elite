# Test Suites

- `Unit` — deterministic engine behavior without external dependencies
- `Integration` — context propagation and subsystem boundaries
- `Performance` — latency and allocation budgets
- `Replay` — historical reconstruction and baseline comparison

Every defect requires a regression test. Every algorithm change requires deterministic replay validation.
