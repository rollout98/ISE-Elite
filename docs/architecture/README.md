# Architecture

This directory contains the approved architectural source of truth for ISE Elite.

- `c4/` — system context, container, and component models
- `deployment/` — runtime and environment topology
- `sequences/` — signal, risk, execution, safety, and replay flows

The architecture baseline is governed by the ADRs in `docs/adr`.
